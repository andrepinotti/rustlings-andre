fn main() {
    let number = "T-H-R-E-E"; // Don't change this line
    println!("Spell a number: {}", number);

    // DONE: Fix the compiler error by changing the line below without renaming the variable.
    // The type of the 'number' variable is str, then we have redeclare her
    let number = 3;
    println!("Number plus two is: {}", number + 2);
}
